"use client";

/**
 * Build the Progress Bar for the endcap component
 *
 * Instructions:
 * - Use React `useState` and `useEffect` to track the user's scroll position.
 * - Dynamically update the width of the progress bar based on the scroll percentage.
 * - Style the progress bar with Tailwind CSS (`h-[2px]`, background gradients, etc.).
 *
 */

export default function ProgressBar() {
  return <div>Progress Bar</div>;
}
