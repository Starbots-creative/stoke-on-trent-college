/**
 * Build the Desktop Navigation using the provided links and logo.
 *
 * Instructions:
 * - Render the `logo` on the left side of the navigation.
 * - Use Array.map() to render each navigation link (use the NavLink component from "../atoms/NavLink.tsx").
 * - Use Tailwind CSS to:
 *    - Make the navigation sticky at the top
 *    - Hide this component on smaller screens
 *
 */

export default function DesktopNavigation({ links, logo }) {
  // console.log(links, logo);

  return <div>Desktop Nav</div>;
}
